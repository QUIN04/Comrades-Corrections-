#include<stdio.h>
#include<math.h>
int main(){
	int a, b,c,d;
	 int x ,y ,ax,cx,by,dy ;
	//the constant and variables above will be used in solving the two set of equations by method of substition.
    	printf("enter a number:");
	scanf("%d", &a);
		printf("enter a number:");
	scanf("%d", &b);
		printf("enter a number:");
	scanf("%d", &c);
		printf("enter a number:");
	scanf("%d", &d);
		ax + by == 0;
	    cx + dy == 0;
	    return 0;
}
